/* Let And Const */

var example = "none";

if (example == 'none') {
    let mangoes = "10"; /* LET is a Like Variable but The data is changable, but only can works inside of action IF ELSE */
    const height = "178"; /* CONST is a Like Variable but it's Unchagable, but only can works inside of action IF ELSE*/
    console.log("I do have ", mangoes, "Mangoes")
    console.log("I my height is ", height, "cm")
}
